/**
 * Data source types available for configuration
 */
export enum DataSourceType {
  POSTGRES = 'postgres',
  MYSQL = 'mysql',
  MONGODB = 'mongodb',
  REDIS = 'redis',
  ELASTICSEARCH = 'elasticsearch',
  CASSANDRA = 'cassandra',
  DYNAMODB = 'dynamodb',
  ORACLE = 'oracle',
  SQLSERVER = 'sqlserver',
  SQLITE = 'sqlite',
}
