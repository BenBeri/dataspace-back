/**
 * Workspace management specific permissions
 */
export enum WorkspaceManagementPermission {
  READ = 'read',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  MANAGE = 'manage',
  
  // Custom workspace management actions (add as needed)
  // INVITE_MEMBERS = 'invite-members',
  // EXPORT_DATA = 'export-data',
}
